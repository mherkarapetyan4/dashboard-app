import { BASE } from './base'
import { COLORS } from './colors'
import { SIZES } from './sizes'

export const theme = {
  base: BASE,
  colors: COLORS,
  size: SIZES,
  components: {
    blockBorder: 'none',
    blockRadius: '.5rem',
  },
}
